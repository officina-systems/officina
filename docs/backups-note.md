# backups/

El directorio `backups/` existe fuera del repositorio en el host local:
`C:\Users\Home\officina\backups\` (no versionado, excluido en .gitignore).

Contenido: snapshots pg_dump y archivos de respaldo operacional.
No forma parte del framework empaquetable.
